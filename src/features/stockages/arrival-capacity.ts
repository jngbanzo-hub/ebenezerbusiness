// Shared display limit; the server must validate every submitted batch independently.
export const MAX_ARRIVAL_PARCELS = 500;
export const ARRIVAL_CAPACITY_MESSAGE = `Un arrivage peut contenir au maximum ${MAX_ARRIVAL_PARCELS} codes colis.`;
