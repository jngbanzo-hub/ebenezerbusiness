"use client";
import {useEffect,useMemo,useState} from "react";import {LoaderCircle} from "lucide-react";import {GlassPanel} from "@/components/design-system";import {authenticatedRead,readJsonOrThrow} from "@/features/auth/authenticated-fetch";import {getSupabaseBrowserClient} from "@/features/agent/supabase";import type {AdminActivity,AdminActivityCategory,AdminRecentActivityResult} from "@/server/admin-recent-activity";
const categories:Array<"TOUTES"|AdminActivityCategory>=["TOUTES","ENCAISSEMENTS","DÉPENSES","QR","STOCKAGE","CAISSE","AGENTS"];const agencies=["TOUTES","COO","FIH","LSHI","KLZ"] as const;
export function AdminRecentActivityPanel({onCount}:{onCount?:(count:number)=>void}){const[result,setResult]=useState<AdminRecentActivityResult|null>(null),[error,setError]=useState(""),[category,setCategory]=useState<(typeof categories)[number]>("TOUTES"),[agency,setAgency]=useState<(typeof agencies)[number]>("TOUTES");useEffect(()=>{authenticatedRead(getSupabaseBrowserClient().auth,"/api/admin/recent-activity").then((response)=>readJsonOrThrow<AdminRecentActivityResult>(response,"Activité récente indisponible.")).then((value)=>{setResult(value);onCount?.(value.count)}).catch((cause)=>setError(cause instanceof Error?cause.message:"Activité récente indisponible."));},[onCount]);const items=useMemo(()=>(result?.activities??[]).filter((item)=>(category==="TOUTES"||item.category===category)&&(agency==="TOUTES"||item.agency===agency||item.agency==="TOUTES")),[result,category,agency]);return <>{error?<GlassPanel className="mt-7 p-6 text-red-200" role="alert">{error}</GlassPanel>:null}{!result&&!error?<LoaderCircle className="mx-auto mt-12 h-7 w-7 animate-spin text-accent"/>:null}{result?<><GlassPanel className="mt-7 p-5"><div className="flex flex-wrap justify-between gap-3"><div><p className="text-sm text-muted-foreground">Activités des dernières 24 heures</p><p className="text-3xl font-bold text-accent">{result.count}</p></div><p className="max-w-xl text-xs text-muted-foreground">Les connexions Agents ne sont pas affichées : aucun journal fiable de connexion/déconnexion n’est actuellement disponible.</p></div><div className="mt-5 grid gap-3 md:grid-cols-2"><Filter label="Catégorie" value={category} values={categories} set={setCategory}/><Filter label="Agence" value={agency} values={agencies} set={setAgency}/></div><div className="mt-4 flex flex-wrap gap-2">{Object.entries(result.sources).map(([name,state])=><span key={name} className="rounded-full border border-white/15 px-3 py-1 text-xs">{name} — {state==="AVAILABLE"?"DISPONIBLE":state==="NOT_RECORDED"?"NON JOURNALISÉ":"INDISPONIBLE TEMPORAIREMENT"}</span>)}</div></GlassPanel><section className="mt-6 space-y-4">{items.length?items.map((item)=><ActivityCard key={item.id} item={item}/>):<GlassPanel className="p-8 text-center text-muted-foreground">Aucune activité pour ces filtres sur les dernières 24 heures.</GlassPanel>}</section></>:null}</>}
function Filter<T extends string>({label,value,values,set}:{label:string;value:T;values:readonly T[];set:(value:T)=>void}){return <label className="text-sm">{label}<select className="mt-1 h-11 w-full rounded-md border border-white/15 bg-ebe-night px-3" value={value} onChange={(event)=>set(event.target.value as T)}>{values.map((item)=><option key={item}>{item}</option>)}</select></label>}
function ActivityCard({item}:{item:AdminActivity}){return <GlassPanel className="p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold text-accent">{item.category} · {item.agency}</p><h2 className="mt-1 text-lg font-semibold">{item.title}</h2></div><time className="text-xs text-muted-foreground">{new Date(item.occurredAt).toLocaleString("fr-FR")}</time></div>{item.trackingCode?<p className="mt-3 font-mono text-sm">Code colis : {item.trackingCode}</p>:null}{item.amount?<p className="mt-2 font-semibold">{item.amount}</p>:null}<p className="mt-2 text-sm">{item.description}</p><p className="mt-2 text-xs text-muted-foreground">Agent/acteur : {item.actor||"Non disponible"}{item.status?` · Statut : ${item.status}`:""} · Source : {item.source}</p></GlassPanel>}
